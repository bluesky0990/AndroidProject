<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nellem.practice04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.mains_profile"></activity>
        <activity android:name=".main.mains_oneperson" />
        <activity android:name=".main.mains_realtime" />
        <activity android:name=".main.mains" />
        <activity android:name=".login.loginRegister" />
        <activity android:name=".login.login" />
        <activity android:name=".loading" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>